{{name}}:
  handler: functions/{{env}}/{{name}}/index.handler
  events:
    - http:
        path: /{{path}}
        method: GET
        cors: ${file(./infra/api-gateway/cors.yml)}
        private: true
